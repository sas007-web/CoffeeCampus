@page
@model CoffeeCampus.Pages.Account.ConfirmEmailModel

@{
    ViewData["Title"] = "Confirm Email";
}
<h1>@ViewData["Title"]</h1>
    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
   {
       <div class="text-danger" >
            @Model.ErrorMessage
         </div>
     }